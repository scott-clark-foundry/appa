---
commit_before_clear: true
---

# To be continued — senior-reviewer cleanup landed; three new doc-lifecycle rules established and waiting to graduate

## Summary (read this first)

The senior-artifact-reviewer agent was rewritten cleanly and dispatched in parallel against three artifacts (code in `assistant/`, the phase 1 spec, and the phase 1 plan + references). During triage Scott established three new lifecycle rules that bounded the cleanup: (a) old plans are frozen and never edited, (b) specs only get touched if a change is load-bearing operationally (not for hindsight-cleanup that would only matter on re-planning), (c) implementer-authored references like `ag-ui-surface.md` are canonical going forward and follow the references-alive-across-phases lifecycle. The cleanup applied to the phase 1 plan and all three reference docs; the spec was left untouched per rule (b); a code-cleanup advisory landed at `assistant/docs/reviews/2026-05-24-pre-phase-1-code-cleanup.md` for the implementer to apply as one pre-Task-1 commit. The mermaid in §03 Design was rewritten twice (mine, then Scott's version); SVG generation via `mmdc` confirms the diagram syntax is valid, so the VSCode preview failure is the extension, not the file. Resumption point: the three new rules live only in this session's chat history and the next session should graduate them to `CLAUDE.md`.

## User's Mindset

Operating mode this session was triage + execution, not design. Scott tightened the doc-edit policy on the fly as findings surfaced; every rule clarification was reactive to a specific question I asked rather than something he raised proactively. He is actively guarding against scope creep ("not updating old plans"), against cleanup-for-cleanup's-sake ("load-bearing only on specs"), and codifying an ownership transition ("ag-ui-surface is canonical because we're not updating old plans"). The implicit posture: the plan is good, the references are good, what matters now is getting them into the implementer's hands clean without re-opening already-settled decisions. He granted me autonomy via "do what makes the most sense here" plus the standing `[[feedback_claude-executes-revisions]]` memory; the few structural calls I made without pre-asking (fold Task 11 into Task 10, cut §07 Risks table entirely, rename `id_factory` → `event_id_factory`, drop `author: planner`) did not draw pushback.

No open user doubts.

## Next steps

1. **Graduate the three new rules to `CLAUDE.md`.** Likely lives in §Phase artifact lifecycle as a new "Edit policy" subsection, or alongside the existing pre-handoff sanitization checklist:
   - Old plans are frozen; never edited after the implementer picks them up.
   - Spec edits gated on load-bearing test: would this change affect a future re-planning decision? If no, leave alone.
   - References authored by the implementer (during phase 0 amendments or later) graduate to canonical and follow the existing references-alive-across-phases lifecycle. `ag-ui-surface.md` is the first instance; future ones inherit the pattern.
2. **Consider a brief convention note** for `assistant/docs/reviews/` housing if more advisories are likely. Could live in `assistant/NOTES.md` or in a one-line README. Optional; the convention is self-explanatory from the one example.
3. **The planner has no further work until phase 1 merges.** Implementer's instance picks up next: they read `assistant/docs/plans/03-transcripts.md`, the three references, and the advisory. They commit the four untracked artifacts as part of their first task on `feat/transcripts`.

## User steering not yet in code

All verbatim. Each tagged with provenance and what it shifted.

- **NEW (this session, doc-edit policy — the headline rule of the session):** "we are not going to update plans. we can update specs IF the change is load bearing - not if it would only make a difference if the spec were planned again. the exception is the CURRENT pending phase 1 plan - this can be reviewed/updated."
  - Triggered the spec being skipped entirely (all 17 reviewer findings on the spec dropped because none rose to load-bearing). Plan + references got the cleanup pass. Should graduate to `CLAUDE.md` § Phase artifact lifecycle.

- **NEW (this session, references canonicality):** "ag-ui-surface is canonical because we're not updating old plans."
  - Triggered the cp of `assistant/docs/references/ag-ui-surface.md` (the implementer's verified copy) into `scratch/docs/references/ag-ui-surface.md` (which had the older planner seed). After cp, applied the cleanup findings in `scratch/`, then cp'd back. Going forward, `ag-ui-surface.md` follows the standard references-alive-across-phases lifecycle. Should graduate to `CLAUDE.md` alongside the no-old-plans rule.

- **NEW (this session, advisory housing):** "docs/reviews/ is find"
  - Triggered creation of `assistant/docs/reviews/` and the placement of the code-cleanup advisory there. If more advisories show up, the convention is established.

- **NEW (this session, mermaid diagnostic):** "the vscode mermaid plugin isn't rendering it still. no error, but it isn't rendering."
  - Triggered the SVG-via-mmdc verification path. The pattern worth keeping: silent non-render in VSCode mermaid preview = extension/cache issue, not syntax. Verify with `npx -p @mermaid-js/mermaid-cli mmdc -i file.mmd -o out.svg` before blaming the diagram.

- **NEW (this session, mermaid version preference):** Scott proposed his own version of the §03 diagram (with function-call signatures restored, blank lines between groupings, `Note over H,A:` instead of `Note over A:`) and I applied it. His phrasing was "is this version any better?" — phrased as a test of whether my prior fix was over-eager, since the original parse error may not have been about parens at all.

## Active state

- **Active skill:** `to-be-continued` is loaded; this file is its output. After commit, no skill active.
- **No in-flight subagents.**
- **No unanswered user question.** Last user input was "to becontinued and commit. merge to main push."
- **TaskList:** not used this session; the work was sequential enough that batched edits + grep-based validation carried the load.

## Pointers

The session's commits in `scratch/` (use `git log -5` for the full picture):

- `06b55af` — agent definition for senior-artifact-reviewer (rewritten from prior untracked state)
- `a925df3` — senior-reviewer cleanup pass on phase 1 plan + three references
- `89c9e5b` — first mermaid fix attempt (later replaced by Scott's version in the next commit)
- (about to land) — Scott's mermaid version + this tbc-notes file

Where to look first when the next session opens:

- `CLAUDE.md` § Phase artifact lifecycle — where the three new rules should land
- `docs/plans/03-transcripts.md` — phase 1 plan, post-cleanup, post-mermaid-swap
- `assistant/docs/reviews/2026-05-24-pre-phase-1-code-cleanup.md` — code advisory waiting for implementer; product-voiced; safe to read
- `docs/references/ag-ui-surface.md` — now scratch-canonical; was previously the older planner seed

Items the next session can ignore unless explicitly drawn to:

- `docs/tbc/tbc-notes-2026-05-24T18-22-24Z.md` — previous session's tbc, captures the dispatched-reviewer setup
- The reviewer punch lists themselves — captured in the prior chat history, no need to re-dispatch unless artifacts change substantially

## Gotchas not yet in CLAUDE.md or memory

- **The load-bearing test for spec edits exists only in this session's chat history.** Until graduated, next-session-me will likely default back to applying reviewer findings to specs reflexively. Rule literally: "we can update specs IF the change is load bearing - not if it would only make a difference if the spec were planned again."

- **`ag-ui-surface.md` is now scratch-canonical AND has uncommitted edits in `assistant/`.** The implementer commits the small cleanup diff when they pick up phase 1. The other three handoff files (`03-transcripts.md`, `vault-write-primitives.md`, `jsonl-transcript-format.md`) are fully untracked in `assistant/` — they're brand new from the implementer's perspective.

- **`id_factory` → `event_id_factory` rename is in the plan but not in any code yet.** The implementer applies it when they write the encoder (Task 6) and recorder (Task 8). The test name `test_recorder_event_id_factory_is_used` lives in the plan's Tested-by block, not in any file yet.

- **VSCode mermaid extension silently fails on syntax `mmdc` accepts.** When investigating a blank mermaid preview, render via `mmdc` first; if it parses, the issue is downstream (extension version, cache, settings).

- **The frozen-plan rule cuts deeper than just "no edits."** It means dead citations in old plans (master progression §X, dead spec links) stay dead. Sanitization sweeps in the next session should grep only the artifacts being touched, not all of `assistant/docs/`, otherwise old-plan hits will pollute the validation signal.
