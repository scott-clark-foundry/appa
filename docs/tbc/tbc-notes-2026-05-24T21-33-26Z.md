---
commit_before_clear: true
---

# To be continued — three lifecycle rules graduated to CLAUDE.md; assistant-side handoff pointer closed the gap

## Summary (read this first)

The three doc-edit rules established in this morning's session are now codified in `CLAUDE.md` § Phase artifact lifecycle as a new "Edit policy after handoff" block. A fresh assistant-side tbc-notes was written to close the gap the prior session flagged: the implementer's most-recently-modified tbc was written before the phase 1 plan landed, so the implementer had no in-tree signal pointing at the plan, the three references, or the pre-Task-1 cleanup advisory in read-order. That gap is now closed by a new file in `assistant/docs/tbc/` that names the read order, calls out advisory-first sequencing, and carries forward the still-true bits from the prior assistant tbc. The pending SVG-fallback decision from the prior tbc was resolved: option (a) (commit + reference from the plan) is now blocked by the freeze rule we just codified, so the SVG ships under `docs/images/` as a local diagnostic artifact with no plan reference. The planner instance has no remaining work until phase 1 merges in `assistant/`.

## User's Mindset

Pure wrap-up mode. Two short directives this session ("update CLAUDE.md with the appropriate skill"; "you can close the gap from here, right?") and a closeout check ("are tbc-notes current and are we committed and ready to clear?"). No exploration, no new design, no opinions surfaced on the SVG or the assistant tbc shape — Scott trusted the judgment calls and would only redirect if something looked wrong. The implicit boundary check in his closeout question is the load-bearing piece: he wants the next session, including the implementer's, to walk in oriented.

No open user doubts.

## Next steps

The planner has no in-flight work. The next time this instance opens, it is most likely to be after the implementer merges phase 1 in `assistant/`, at which point the work is:

1. **Reconcile reference-doc amendments from phase 1.** Read `assistant/docs/references/jsonl-transcript-format.md` and `assistant/docs/references/vault-write-primitives.md` (both will have `last-verified` set, sample lines pasted, AG-UI capture-path branch chosen). Cp the implementer's versions into `scratch/docs/references/` per the references-alive lifecycle.
2. **Update the master progression plan.** Append the `**Artifacts.**` line for phase 1 once it merges, marking spec + plan + merged status.
3. **Start phase 2 planning** (semantic recall over JSONL transcripts). Likely entry point: `journey-planning` skill to define what "semantic recall works" means user-facing, then `writing-plans` for the HTML draft.

If the next session opens *before* phase 1 merges (e.g., for an unrelated scratch-side task), there's nothing time-sensitive on the planner side.

## User steering not yet in code

Nothing this session. Every directive Scott gave was executed inside the session:
- CLAUDE.md edit applied.
- Assistant tbc-notes written and verified to land alongside the four untracked phase 1 artifacts in `git status -C ../assistant`.
- SVG decision made (commit under `docs/images/` per the freeze rule).

## Pointers

- `CLAUDE.md` § Phase artifact lifecycle — the new "Edit policy after handoff" block is the canonical home for the three rules; tbc references are now redundant.
- `assistant/docs/tbc/tbc-notes-2026-05-24T19-55-19Z.md` — handoff pointer the implementer reads first; names the four phase 1 artifacts + the cleanup advisory in read order with advisory-first sequencing.

Items the next session can ignore unless explicitly drawn to:

- `docs/tbc/tbc-notes-2026-05-24T19-42-28Z.md` — the prior session's tbc; its open SVG decision is now resolved and its "graduate the rules to CLAUDE.md" next-step is complete.
- `docs/images/03-transcripts-design.svg` — the mermaid fallback render; committed for reuse if the diagram ever thaws but not referenced from any doc.

## Gotchas not yet in CLAUDE.md or memory

- **The freeze rule blocks fallback-image-link patterns on frozen plans.** When the SVG decision came up this session, option (a) (commit + reference from the plan) was blocked by the rule we'd just codified. If a similar "add a small thing to a plan after handoff" temptation surfaces in a future session, the rule cuts the same way. The escape hatch is to capture the addition in `references/` (which is alive) or `NOTES.md` (authored fresh in `assistant/`) rather than amending the plan.

- **`docs/images/` exists in `scratch/` now but has no documented convention.** This session committed one SVG without a README or directory note. Future diagnostic renders can land there; if the directory grows past a few artifacts, the convention probably deserves a one-line note in `CLAUDE.md` § Project context.
