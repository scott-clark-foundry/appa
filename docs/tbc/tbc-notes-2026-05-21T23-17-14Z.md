> **Uncommitted.** This note and the entire session's revision live only in the working tree (`docs/plans/` is untracked; the old HTML shows as deleted). A `/clear` survives it, but commit before any branch switch or clone.

# To be continued — portfolio plan revised, converted to markdown, one revert pending

## Summary (read this first)

The portfolio plan went through several revision rounds this session and changed *form*. It is no longer an atelier-plans HTML document: the canonical artifact is now `docs/plans/python-llm-app-progression.md`, plain markdown that Scott exported via the atelier-plans "Copy markdown" web UI. The HTML was moved to `docs/plans/html/` as a frozen authoring archive, and the old `plans/portfolio/` path is gone. Conversion was verified faithful (frontmatter intact, I1-I8, ADR-001..005, zero em-dashes, all ten phases). The plan's *content* also shifted materially: multi-principal support cut entirely, the ADR set consolidated from eight to five, terminology settled on "phase" (the unit) plus "branch" (the git artifact), trace moved to Pydantic Logfire, provider choice demoted from an ADR to a config detail. Exactly one change needs undoing next session: the I8 external-writes guardrail invariant was dropped as YAGNI near the end, and Scott reversed that call.

## User's Mindset

Scott is in tightening / finalizing mode, not exploration. Converting the doc to markdown signals the HTML authoring phase is done and the plan is stabilizing toward a buildable spec. He runs careful, itemized review passes and expects each point addressed precisely and verified (he caught fabricated-looking citations, ungrounded statistics, anachronisms, stale cross-references). He cuts speculative scope hard: this session he dropped multi-principal as "speculative scope ... not worth carrying through ten phases," the Letta carve-out (no phase used it), and the I8 guardrail — then reversed the I8 call. He is optimizing for a coherent, honest, minimal plan with no dead text and no unverified claims. The `assistant/` repo still does not exist; this is all still planning. Plan status is "draft · in planning."

## Next steps

1. **Revert the I8 drop** in `docs/plans/python-llm-app-progression.md`. Restore the external-writes guardrail as **I8**; renumber the current I8 (the "App code never imports a provider SDK directly" invariant) back to **I9**, so the list reads I1-I9. Update the Init section's DOD reference from I8 to I9. The exact I8 text to restore is in "User steering" below.
2. Decide whether the archived HTML at `docs/plans/html/python-llm-app-progression.html` gets patched to match, or stays a pre-conversion snapshot. Default: leave it; the `.md` is canonical and the HTML is reference-only.
3. Commit the changeset (the full revision, the HTML→MD move, this tbc-note) — currently all uncommitted on `main`.

## User steering not yet in code

- **NEW — verbatim:** "You're right on I8, my bad. Let's please run /to-be-continued and leave the note to revert that." **SUPERSEDES** this session's decision to drop I8 as YAGNI. The I8 invariant to restore (the generalized form it held just before the drop — Bluesky is read-only now, so it must *not* name Bluesky writes):

  > Writes to external systems (any social-platform or third-party API write a future tool introduces) are scoped to bot or sandbox accounts owned by the author. Reads can be global, subject to platform terms and rate limits. Larger public-figure accounts that opt into bot interaction can be allowlisted; default-deny on writes otherwise. Protects against accidental notification spam to non-consenting users and keeps the portfolio defensible.

- **NEW — verbatim:** "I will have exported the html to markdown via the web ui and saved it in docs/plans/python-llm-app-progression.md ... i've intentionally moved the html to docs/plans/html/." The markdown is canonical going forward; future edits are plain markdown edits to the `.md`, not atelier-plans HTML edits.

## Active state

Nothing mid-flow. No subagents in flight, no unanswered question. Every other directive Scott gave this session (the four architecture decisions, the six accuracy fixes) is already applied to the `.md`. Only the I8 revert is outstanding.

## Pointers

- `docs/plans/python-llm-app-progression.md` — the canonical plan. All edits go here.
- `docs/plans/html/python-llm-app-progression.html` — frozen atelier-plans authoring HTML, pre-conversion snapshot (~2740 lines with inlined CSS/JS). Reference only; already one revert behind.
- `docs/tbc/tbc-notes-2026-05-21T21-18-11Z.md` — prior tbc-note. Its Addendum specified an anti-framework / pydantic-ai cleanup pass; that pass was completed at the start of this session. Historical now.
- `/Users/scott/.claude/plans/edits-to-apply-reactive-galaxy.md` — the plan-mode plan for this session's largest revision round. Fully executed. Historical.
- Memory: two feedback memories added this session (`no-speculative-scope`, `claude-executes-plan-revisions`) load automatically via MEMORY.md.

## Gotchas not yet in CLAUDE.md or memory

- The plan is **markdown now**. Do not edit the HTML in `docs/plans/html/` expecting it to be canonical — it is a frozen archive and is already behind by the pending I8 restore.
- The whole session's work is **uncommitted**: `git status` shows the old HTML deleted and `docs/plans/` untracked. Nothing is on `main` yet.
- Numbering was churned this session: ADRs went 8 → 5 (ADR-001..005), invariants lost two slots (now I1-I8). The I8 revert brings invariants back to I1-I9. Earlier tbc-notes reference the *old* numbering — do not trust ADR/invariant numbers from them; read the `.md`.
- "phase" = the unit of progress (ten phases); "branch" = the git feature branch delivering a phase. Deliberate two-word split, not interchangeable.
