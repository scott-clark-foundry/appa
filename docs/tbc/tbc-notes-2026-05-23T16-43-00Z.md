# To be continued — leaner plans + references lifecycle landed; next: phase 0 impl review, external phase 1 plans, phase 1 planning

## Summary (read this first)

The planner workflow got a structural upgrade this session. The `writing-plans` skill no longer prescribes implementation code in task bodies: it now requires contracts (signatures, schemas, fixtures, test-behavior assertions) and links to a new `docs/references/<name>.md` system that carries cross-phase context the plan body no longer holds. `CLAUDE.md` "Phase artifact lifecycle" codifies the lifecycle: planner seeds, implementer refines, planner reconciles at the next phase. Empty `docs/references/` scaffolding landed in both `scratch/` and `assistant/`. The prior scale-back TODO in `docs/todos.md` is superseded. Four commits in `scratch/` + one in `assistant/` (plus this tbc-notes commit) close out the change. Phase 1 (transcripts) is the first plan that will exercise the new format.

## User's Mindset

Scott is shifting from planner-workflow housekeeping back to product work. He's holding two follow-ups for the next session: review what's actually landed in the `assistant/` phase 0 implementation so far, and bring in external phase 1 plans he drafted with a different tool. Phase 1 brainstorming and planning will follow that, using the now-leaner writing-plans + new references model as the proving ground. He is not currently asking for anything else; the directive is execute → review → import → plan.

## Next steps

1. **Review phase 0 implementation.** `assistant/` is on branch `init/scaffold` (not `main`). Several commits have landed (README/LICENSE/CHANGELOG/NOTES, CI, app, agent, fixtures, etc.). There is a pre-existing `M .env.example` and an untracked `dev/` directory that this session did not touch. Walk the recent log + diff against `main` to see how far the 9-task plan got, and where the implementer made choices that diverged from the (over-prescribed) plan.
2. **Import Scott's external phase 1 plans.** He'll bring in plans drafted with a different tool. Read them with `brainstorming` voice, not `writing-plans` voice — they are likely scope/intent shaped, not task-shaped.
3. **Plan phase 1 (transcripts).** First plan under the new format. Prereqs: phase 0 merged to `assistant/main` (verify), `docs/plans/python-llm-app-progression.md` `#l1` re-read, `assistant/docs/references/*` re-read (will be empty; that's fine). Likely first reference doc: `vault-write.md` (manifest + staging + provenance primitives introduced this phase, reused forever).

## User steering not yet in code

- **NEW (this session's trigger, verbatim):** "Let's think through what that looks like. Without the details we'll need to give assistant.claude access to tech stack/architecture info. two options, embed in plan or create additional references. i'm inclined towards the additional refernces (when needed )." Drove the whole references design.
- **NEW (decided via AskUserQuestion):** Reference docs live at `assistant/docs/references/` (mirrored at `scratch/docs/references/`). Authoring model is planner seeds, implementer refines (next-phase planning reconciles). Now codified in `CLAUDE.md` and the skill; capture here for the throughline.
- **NEW (resumption directive, verbatim from `/to-be-continued` args):** "the commit. when we come back we're going to take a look at the phase 0 implementation and talk about some plans i had drawn up for phase 1 using a different tool - then we'll plan our phase 1." Naming this verbatim because "a different tool" is load-bearing: the external plans are an *input* to phase 1 planning, not the plan itself.
- **STANDING (reaffirmed implicitly):** Phase 0's over-prescribed plan stays as the transitional artifact. New rules apply phase 1 onward.

## Active state

Nothing mid-flow. No subagents in flight. Plan-mode work concluded with approval and full execution. Last user action was the `/to-be-continued` invocation.

## Pointers

- `.claude/skills/writing-plans/SKILL.md` — the revised skill. Read `## Reference Docs` and `## Task Structure` before authoring phase 1.
- `CLAUDE.md` §Phase artifact lifecycle — references lifecycle is codified here.
- `/Users/scott/projects/assistant/` (branch `init/scaffold`) — phase 0 in-flight work; `git -C ... log --oneline` shows progress.
- `~/.claude/plans/let-s-think-through-what-encapsulated-milner.md` — the plan that drove this session's changes (approved + executed).
- `docs/plans/python-llm-app-progression.md` `#l1` anchor — phase 1 (transcripts) scope to plan against.
- `docs/tbc/tbc-notes-2026-05-23T01-49-12Z.md` — prior session (phase 0 spec + plan + this session's TODO).
- `~/.claude/projects/-Users-scott-projects-scratch/memory/feedback_plans-no-code.md` — the feedback memory that motivated the scale-back. Its "Until the writing-plans SKILL.md itself is revised..." paragraph is now outdated; consider trimming when next memory-edit pass happens.

## Gotchas not yet in CLAUDE.md or memory

- **`assistant/` is on `init/scaffold`, not `main`.** Phase 0 hasn't merged yet. Pre-existing `M .env.example` and untracked `dev/` were left alone this session.
- **The project-local writing-plans skill diverges from upstream.** `~/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0/skills/writing-plans/SKILL.md` is the unmodified upstream; the scale-back rule is scratch-only. Don't propagate automatically.
- **The new "Reference Docs" rule applies to authoring, not retro-reading.** Phase 0's plan still has full code; reviewing phase 0 implementation against it is fine because phase 0 is the transitional artifact. The compare-to-contract discipline starts at phase 1.
- **Background-shell PATH stripping (carried forward).** Subprocesses spawned from background-Bash see a minimal PATH; pass env directly via `/usr/bin/env PATH=… …`. First place to look if a background command exits 127.
- **MIT attribution risk (carried forward).** Atelier-plans assets live in `writing-plans/assets/` without explicit attribution. Scratch is private so this is dormant; activates if the assets ever move into `assistant/`.
