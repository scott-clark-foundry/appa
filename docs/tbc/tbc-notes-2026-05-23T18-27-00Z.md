---
commit_before_clear: true
---

# To be continued — phase 0 reviewed; AG-UI refactor in flight; next: critique Scott's draft phases 1-5 plan

## Summary (read this first)

Phase 0 review came back green: all 9 tasks landed, every CI gate passes locally in `assistant/`, with two implementer divergences worth knowing (`defer_model_check=True` on the Agent so import doesn't blow up when `OPENAI_API_KEY` is absent; an extra `B008` ruff-ignore commit because `Depends()` as a default arg trips the lint). Loose ends I flagged mid-review have already been resolved by Scott + assistant.claude: the `gpt-5-nano` default is now committed in `config.py`, and `dev/` is tracked. Mid-evaluation, Scott pivoted: he and assistant.claude are now actively refactoring the chat endpoint to use **AG-UI** instead of the hand-rolled SSE shape (`event: token` / `event: done`) that phase 0 shipped. This is a structural change — the SSE contract that the phase 0 plan §07 risks called out as the thing phase 1's CLI would have to agree with is being replaced before phase 1 starts. The next session's task is NOT phase 1 planning per se: Scott will bring a draft *phases 1-5* implementation plan and wants my feedback on it. That supersedes the prior tbc-notes' "plan phase 1 next" framing.

## User's Mindset

Scott is in execution mode on two fronts at once. Assistant.claude is the active worker on AG-UI in `assistant/` (live in `dev/assistant/`, not yet committed). Scott himself is operating as a planner: he has drafted phases 1-5 with a different tool and wants this instance to *critique*, not author. The optimization target for the upcoming session is "is this plan good?" — not "what should the plan be?" Treat his draft as the spec; my job is to apply the leaner writing-plans + references discipline as a lens, flag where the draft over-prescribes (function bodies, full source) or under-specifies (missing contracts, missing reference-doc candidates), and surface anything the AG-UI shift invalidates. He has not expressed doubts about the AG-UI choice; treat it as decided.

## Next steps

1. Wait for Scott. He'll signal when the AG-UI refactor in `assistant/` is done and he's ready to share the draft phases 1-5 plan.
2. When he returns, read his draft as an input artifact (likely brainstorming-voiced, not plan-voiced). Do not rewrite it — give feedback against `.claude/skills/writing-plans/SKILL.md` (especially §Reference Docs and §Task Structure) and against the now-AG-UI-shaped phase 0 surface.
3. While reviewing, watch for reference-doc candidates that span phases 1-5: vault-write primitives (manifest/staging/provenance), the AG-UI event surface as the new inter-phase contract, fixture frontmatter conventions, test-seam patterns.
4. Re-read phase 0's plan §07 Risks and §08 Acceptance with AG-UI in mind — the "SSE response shape disagrees with phase 1 client" risk is being neutralized at the source rather than mitigated in phase 1.

## User steering not yet in code

- **NEW, verbatim (this session's pivot):** "actually, assistant.claude and I are busy right now refactoring to use AG-UI instead of hand-rolled sse. I'll let you know when we're done." Load-bearing because phase 0's `app.py` ships the hand-rolled shape (`event: token` / `event: done`) and phase 0's plan §07 Risk 3 anticipated phase 1 negotiating with it. The negotiation is being skipped by replacement.
- **NEW, verbatim (resumption directive):** "We'll come back and take a took at a draft phase 1 -5 implementation plan that I'd like your feedback on." Two shifts vs. the prior tbc-notes: (a) scope expands from phase 1 alone to phases 1-5; (b) my role is *reviewer*, not author. Scott is bringing the draft.
- **SUPERSEDES** the prior tbc-notes' Next-Step #3 ("Plan phase 1 (transcripts). First plan under the new format."). The new format still applies, but as a critique lens, not an authoring directive — at least for this round.
- **STANDING:** The "planner seeds, implementer refines" references lifecycle (now codified in `CLAUDE.md` and the writing-plans skill) still governs; AG-UI doesn't change it.

## Active state

Nothing mid-flow in this instance. No subagents in flight. Real work is happening in the sibling `assistant/` repo under assistant.claude — not visible to this context. Last user action was the pivot + `/to-be-continued` invocation.

## Pointers

- `docs/tbc/tbc-notes-2026-05-23T16-43-00Z.md` — prior session's notes (leaner-plans + references-lifecycle landing). Its "Next steps #2 and #3" are partially superseded by this session; "Next steps #1" (review phase 0 implementation) is done.
- `/Users/scott/projects/assistant/` on branch `init/scaffold` — phase 0 in-flight; not merged to `main`, no GitHub remote yet, so the `init/scaffold` CI run has never actually executed remotely.
- `/Users/scott/projects/assistant/dev/assistant/` — untracked, this is where the AG-UI refactor work lives. Don't read it cold next session; let Scott tell us what shipped.
- `/Users/scott/projects/assistant/assistant/app.py` — current hand-rolled SSE; the thing being replaced. Worth diffing once AG-UI lands to see the new contract.
- `/Users/scott/projects/scratch/.claude/skills/writing-plans/SKILL.md` §Reference Docs and §Task Structure — the lens for critiquing Scott's draft.
- `docs/plans/python-llm-app-progression.md` `#l1` through `#l5` — the master phases-1-5 scope to check Scott's draft against.

## Gotchas not yet in CLAUDE.md or memory

- **Phase 0 acceptance is not yet met end-to-end.** `init/scaffold` has not merged to `main`, `assistant/` has no GitHub remote, so the workflow at `.github/workflows/ci.yml` has never run. CI is green *locally*, not *remotely*. Don't assume phase 0 is "shipped" — it's "code-complete on a feature branch."
- **AG-UI invalidates the phase 0 plan's §03 Design diagram and §07 Risk 3.** Both reference the hand-rolled SSE shape. The plan stays as the as-handed-over snapshot (per `CLAUDE.md` phase artifact lifecycle), but the *live* code in `assistant/` will diverge. That's fine and expected; just don't quote the phase 0 plan's SSE shape as authoritative when reviewing phase 1+ drafts.
- **pytest is chatty over the network when `.env` has `LOGFIRE_TOKEN`.** The `logfire-4.33.0` pytest plugin auto-reports each test run to Logfire. Not a code bug, not a defect in `configure_logging`; it's the plugin doing its thing. Re-run with `LOGFIRE_TOKEN=` to silence. Worth flagging if test telemetry becomes load-bearing later.
- **Scott edits the implementer's output directly.** Between my review pulling state and writing the tbc, the `gpt-5-nano` change moved from "uncommitted in `.env.example`" to "committed in `config.py`" (`f056538`), and the `dev/` directory got tracked (`fb63ee0`). Don't snapshot loose-end inventories as authoritative for more than a few minutes.
- **The external phases-1-5 plan was drafted with a different tool.** Read it as scope/intent input, not as a writing-plans-shaped artifact. Critique its substance, not its formatting.
