> **Single current tbc-notes.** This file supersedes `tbc-notes-2026-05-23T00-32-20Z.md` (written about 75 minutes earlier). Next session reads only this file; the prior is historical.

> **Uncommitted at write time.** Substantial: prior session's skill absorption + this session's skill polish + phase artifact lifecycle codification + phase 0 spec + phase 0 plan (HTML + markdown) + docs/todos.md + the plan copy in `../assistant/`. Commit will be split into focused chunks.

# To be continued — phase 0 specced and planned end-to-end; next: revise writing-plans, then phase 1 brainstorm

## Summary (read this first)

Two big things landed this session. First, the planning toolchain (brainstorming → writing-plans → execute) was audited end-to-end and small frictions fixed: brainstorming's stale path to `visual-companion.md`, writing-plans' under-qualified template-copy path, the silent ownership of the Markdown handoff step, and two orphaned reviewer-prompt files. Second, phase 0 (`init/scaffold`) was specced and planned all the way through to a markdown handoff. The spec lives at `docs/specs/00-init-scaffold.md`; the plan lives at `docs/plans/00-init-scaffold.md` and has already been copied to `../assistant/docs/plans/00-init-scaffold.md`, which is the worker instance's live execution document. Scott reviewed the plan and flagged it as over-detailed: the implementer (another full Claude Code instance, not a subagent) was left nothing to think about or decide. That feedback is captured as a memory ([[plans-no-implementation-code]]) and a TODO ([docs/todos.md](todos.md is the location)); the phase-0 plan stands as a transitional artifact. The progression-plan ↔ skills interface was also locked in: per-phase artifacts at `docs/{specs,plans}/NN-<slug>.md`, plan is product-voiced from the start, handoff is a literal `cp` into `assistant/`. CLAUDE.md got a new "Phase artifact lifecycle" section.

## User's Mindset

Scott has now exercised every part of the planning loop on a real phase, end to end. He found one important thing wrong (writing-plans over-prescribes) and one important thing right (the brainstorming + writing-plans + HTML iteration shape works). He wants to fix the over-prescription before phase 1 so phase 1 onward uses the leaner format. He explicitly opted out of subagent-driven-development for phase 0 ("no review cycle in assistant"); the implementer instance is a full Claude Code with its own context, so it should make implementation choices itself rather than being treated like a subagent that needs spoon-feeding. The two-instance model (planner in `scratch/`, implementer in `assistant/`) is now real, not theoretical.

## Next steps

1. **Revise writing-plans/SKILL.md per docs/todos.md.** The "Complete code in every step" rule is the line that needs to change. Plans should document interfaces, behavior contracts, test cases, acceptance criteria, and data models/schemas. No function bodies, no step-by-step file rewrites. The TDD step pattern still applies; the test code itself is up to the implementer. Update `references/DESIGN.md` and `EXAMPLE-rfc.html` if they show prescriptive examples. Time-box this: one session, not a deep redesign. The phase-0 plan stays as-is and is the last over-prescribed plan we ship.
2. **Begin phase 0 implementation in `../assistant/`.** Scott opens a Claude Code instance there, points it at `docs/plans/00-init-scaffold.md`, and works through the 9 tasks. No subagent-per-task. Plan is read-and-execute. The plan is over-detailed (transitional artifact); the implementer instance should feel free to deviate from prescribed code if it has a better idea, treating the plan's code as illustrative rather than authoritative.
3. **Brainstorm phase 1 (transcripts) once phase 0 lands.** Prereq: writing-plans is revised (Step 1) and phase 0 is merged to `assistant/main` (Step 2). Phase 1 plan should be leaner: interfaces, acceptance, fixtures, not full function bodies. Phase 1 covers durable session-transcript persistence, vault-write primitives (manifest + staging + provenance markers), `[cancelled]` marker on disconnect, persistence-round-trip and cancellation evals. The vault-write primitives are introduced in phase 1 and reused forever (§10 cross-cutting patterns); their interface design is the architectural moment of phase 1.

## User steering not yet in code

- **NEW — verbatim (sets next session's #1):** "Plan looks too good. We've left nothing for the implementer to think about or decide. Make a TODO that we need to scale back the level of detail out of writing-plans - certainly there should be no implementation code other than pershaps models/schemas." Now in `docs/todos.md` and saved as memory [[plans-no-implementation-code]]. The TODO names the specific SKILL.md line ("Complete code in every step") and the timing ("before phase 1").
- **NEW — verbatim (decided the implementer's review model):** "let's go with phase 0, but keep it tight with no review cycle in assistant. this should be straightforward." For phase 0, the implementation instance executes the plan linearly without subagent-driven-development's per-task subagent + two-stage review. Whether later phases adopt the review cycle is open; phase 0 is the test case for the leaner model.
- **NEW — implicit (Scott's edits to the markdown after pasting):** Scott edited the YAML frontmatter and a few formatting details (mermaid diagram simplified, callouts converted to `> [!TIP]` / `> [!NOTE]` GitHub-flavored admonitions). The implementer should treat the markdown file as canonical, not the HTML draft. The HTML at `docs/plans/html/00-init-scaffold.html` is the iteration artifact; the markdown is what was handed over.
- **STANDING (reaffirmed by the absorption work):** Reusable-on-future-projects rule for skills. Now applies to the revised writing-plans Scott will produce in Step 1.

## Active state

Nothing mid-flow. No subagents in flight. Phase 0 plan is over-detailed but locked (transitional artifact); the markdown is copied to `assistant/docs/plans/00-init-scaffold.md` and ready for the implementation instance to consume. Last user-facing action: this tbc-notes write + commit, both at Scott's instruction.

## Pointers

- `docs/specs/00-init-scaffold.md` — phase 0 spec; product-voiced; lives in scratch only.
- `docs/plans/00-init-scaffold.md` — phase 0 plan, canonical markdown; Scott pasted the Copy-markdown export and edited frontmatter / admonitions.
- `docs/plans/html/00-init-scaffold.html` — phase 0 plan HTML draft; kept as the iteration artifact. Was Scott's authoring surface.
- `../assistant/docs/plans/00-init-scaffold.md` — live execution document the implementer instance reads. Currently identical to `scratch/`'s copy; will diverge as tasks tick off.
- `docs/plans/python-llm-app-progression.md` — has `#l0`…`#l10` anchors now; §08 init has an Artifacts line linking to spec and plan; everything else unchanged.
- `docs/todos.md` — the writing-plans scaling-back TODO with the specific SKILL.md line to revise and the "before phase 1" timing.
- `CLAUDE.md` — has a new "Phase artifact lifecycle" section codifying the handoff mechanism. Read it before brainstorming phase 1.
- `~/.claude/projects/-Users-scott-projects-scratch/memory/feedback_plans-no-code.md` — the new feedback memory.
- `.claude/skills/writing-plans/SKILL.md` — the file Step 1 revises. Specifically the "Task Structure" section and the "Complete code in every step" wording.

## Gotchas not yet in CLAUDE.md or memory

- **The phase-0 markdown plan is over-detailed by the new rule.** It contains complete implementation code for every module. Treat as a transitional artifact; the implementer should feel free to make different choices where the code looks wrong (e.g., pydantic-ai API drift, structlog config quirks). Don't measure the plan-revision skill against it.
- **The phase-0 plan has a known pydantic-ai-API-drift caveat baked in.** The HTML's Tasks section has a `data-kind="caveat"` callout warning the implementer that current method signatures may differ from what `uv sync` resolves. Verbatim in the markdown form too (line ~158).
- **Scott edited the YAML frontmatter in `docs/plans/00-init-scaffold.md`.** Specifically: `phase: "0 of 10"` (string with the total, not just `0`), `progression: "progression #l0"` (label, not the full path), and tags as a flow-style list. If you re-generate the plan from HTML and the frontmatter shape differs, prefer Scott's edits. They are the canonical shape for plan frontmatter in this repo going forward.
- **The `vault/evals/phase-0/smoke-001.md` fixture file must be force-added** (`git add -f`) because of the vault `.gitignore` un-ignore pattern. The plan's Task 7.6 calls this out; the implementer will see it. Worth remembering if a future phase tries to track a similar file under `vault/`.
- **Background-shell PATH stripping (carried forward).** Subprocesses spawned from background-Bash see a minimal PATH; pass env directly via `/usr/bin/env PATH=… …`. First place to look if a background command exits 127.
- **MIT attribution risk carried forward.** Atelier-plans assets are now in `writing-plans/` without explicit attribution. Scratch is private so this is dormant; activates if the assets ever move into `assistant/` during phase work.
