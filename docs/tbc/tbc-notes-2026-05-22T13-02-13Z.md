> **Uncommitted.** The five-skill stack and the progression-plan Intent revision live only in the working trees. `scratch/` has the modified progression plan plus three untracked skill dirs; `assistant/` has zero commits and all content untracked. Commit before any branch switch or clone.

# To be continued — portfolio went two-repo; custom planning skill stack built

## Summary (read this first)

This session turned the portfolio from a conceptually-single project into an explicit two-repo system and built the machinery to run it. The private `scratch/` repo is the planning workspace; a new public `assistant/` repo (created this session, still zero commits) is the product. The handoff between the two is now a formal five-skill pipeline: `phase-planning` then `architecture-docs` then `epic-breakdown` on the planning side, `implementation-planning` then `executing-plans` on the implementation side. The `journey-planning` / `journey-testing` skills were evaluated against the project and rejected; verification folds into `executing-plans` instead. The skill stack is built and passed format, boundary, and activation-routing checks, but it has never been run. The next real work is using it to plan phase 1, which is also its first live test. Separately, the progression plan's Intent section was rewritten as a pure vision statement with goals and non-goals consolidated into sections 02 and 03; that section-by-section review only reached section 01.

## User's Mindset

Scott is finalizing the *process* while the *product* is still entirely unbuilt (no assistant code exists). He is optimizing for: a lightweight process (he rejected the heavy multi-tier SDLC chains in his sister projects because they over-decompose work and produce routine tests); strict public/private separation (career framing must never reach the public repo); and handing assistant.claude epic-level tasking that it then plans its own implementation against. He wants things said plainly — he asked mid-session to "say it straight in one pass." He runs careful itemized review and catches loose threads. One thread is still open: he started "let's go over the big picture plan section by section together" and we were pulled away after section 01.

## Next steps

1. Decide commits. `scratch/`: the progression-plan Intent revision (modified, uncommitted) plus three untracked skill dirs. `assistant/`: its first-ever commit (the two skill files). Two separate repos, so two commits. Scott was asked and had not answered.
2. Optionally draft `assistant/CLAUDE.md` — flagged as an out-of-scope follow-on; it tells assistant.claude about its two skills, the relaxed product voice, and the public-repo rule. Scott was asked; unanswered.
3. The live dry run: use `phase-planning`, then `architecture-docs`, then `epic-breakdown` to plan phase 1 (`feat/transcripts`). This is the real test of the stack.
4. Resume the section-by-section review of `docs/plans/python-llm-app-progression.md`. Only section 01 (Intent) was covered; sections 04 Invariants through 14 References are unreviewed.

## User steering not yet in code

Most steering this session landed in committed `CLAUDE.md` sections, the approved plan file, or the skill files themselves — see Pointers. What is verbatim and worth preserving intact:

- **NEW — verbatim:** "i don't think those journey* skills are right for our project." A non-deterministic LLM app is judged by evals, not exact-match `Verify` commands. This is baked into the skill design (no `journey-*`; verification folded into `executing-plans`) but is not in `CLAUDE.md` or memory — it lives only in the plan file and the skills.
- **NEW — verbatim:** "i want assistant claude given architecture, epics, any tech stack details. it doesn't have to be in that format - that's just the level of informaiton the assistant.claude should get." The format is flexible; the epic *altitude* is the point. Baked into `epic-breakdown`.
- **NEW — verbatim:** "Most of your output will be assistant.claude's input - except for the input assistant.claude plans for themselves." The exception (assistant.claude planning for itself) is exactly the `implementation-planning` skill: epic into tasks.

## Active state

Nothing mid-flow. All six session tasks completed. No subagents in flight. Two user questions are unanswered, both from the final turn: (1) commit the skill stack now? (2) draft `assistant/CLAUDE.md`?

## Pointers

- `~/.claude/plans/ok-i-don-t-think-temporal-swing.md` — the approved plan for the five-skill stack. Full design rationale, the pipeline diagram, the decisions baked in, the verification section.
- `docs/plans/python-llm-app-progression.md` — the progression plan. Sections 01-03 revised this session (uncommitted); sections 04-14 not yet reviewed.
- `.claude/skills/{phase-planning,architecture-docs,epic-breakdown}/SKILL.md` — the three planning-instance skills (run in `scratch/`).
- `/Users/scott/projects/assistant/.claude/skills/{implementation-planning,executing-plans}/SKILL.md` — the two assistant.claude skills (run in `assistant/`).
- `CLAUDE.md` — the Public/private boundary and Build coordination sections (committed: `454591f`, `948977a`).
- Memory: `project_public-private-boundary.md`, `project_build-coordination.md`.
- `docs/tbc/tbc-notes-2026-05-21T23-17-14Z.md` — prior tbc-note (the I8 revert, done and committed `7347d28`).

## Gotchas not yet in CLAUDE.md or memory

- The five-skill stack is built and verified for *format* but is **unproven** — it has never been run. The skills look finished; they are not yet exercised. Treat phase-1 planning as a shakedown, expect to revise the SKILL.md files.
- `scratch/` is 9 commits ahead of `origin/main`, unpushed.
