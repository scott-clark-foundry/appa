> **Single current tbc-notes.** This file supersedes `tbc-notes-2026-05-22T23-59-14Z.md` (written 33 minutes earlier). Next session reads only this file; the prior is historical.

> **Uncommitted at write time.** Substantial: this session's atelier-plans absorption plus the prior session's pivot are all still on the working tree. Tracked deletions for `.claude/skills/atelier-plans/...`, modifications to brainstorming and writing-plans SKILL.md, untracked `.claude/archive/`, `writing-plans/assets/`, `writing-plans/references/`, `writing-plans/html-authoring.md`.

# To be continued — atelier-plans absorbed into writing-plans; next: skill pass + progression interface

## Summary (read this first)

The atelier-plans skill no longer exists as a discrete skill. Its functionality (HTML drafting + iteration loop) is now baked into `writing-plans`, mirroring how `brainstorming` bundles Visual Companion. The original skill moved untouched to `.claude/archive/atelier-plans/`; `writing-plans` got its own copies of the assets (template, CSS, JS) plus references (DESIGN.md + three example HTMLs) plus a new sibling `html-authoring.md` adapted from `atelier-plans/SKILL.md`. An earlier-session direction to merge `brainstorming` into `writing-plans` was floated, researched, and rejected; the two-skill chain (brainstorming → writing-plans → subagent-driven-development) holds. Phase 1 (transcripts) spec is still pending. Next session: review the new state of `brainstorming` and `writing-plans` end-to-end, then think through how progression-plan phases interface with the brainstorming + writing-plans pipeline.

## User's Mindset

Still in scaffold / process mode; no app code exists. Scott is shaping the planning toolchain before using it to spec phase 1. He is optimizing for skills he can actually run AND reuse on future projects — that reusable-on-future-projects rule drove the absorption (writing-plans is self-contained rather than depending on a separately-loaded skill). Two distinct surfaces for two skills: Visual Companion for brainstorming's spec dialog, HTML iteration for writing-plans' plan drafting. He wants ONE current tbc-notes file on resumption (this file), and is treating new tbc-notes as a successor to the prior file, not an addendum on top of it.

## Next steps

1. **End-to-end pass through the new skills.** Read `brainstorming/SKILL.md`, `writing-plans/SKILL.md`, and `writing-plans/html-authoring.md` cold. Check the offer messages, the iteration loops, the cross-references. Confirm the asset paths in `writing-plans/SKILL.md` and `html-authoring.md` resolve to the local `assets/` and `references/`. If time, smoke-test by invoking `writing-plans` on a throwaway target and watching: does the HTML offer fire cleanly as its own message? Does the template copy work? Does the Markdown handoff still trigger correctly?
2. **Design the progression-plan ↔ skills interface.** `docs/plans/python-llm-app-progression.md` is a 10-phase plan; each phase should become its own spec → plan → execution cycle. Open: where do per-phase artifacts live (`docs/specs/<phase>.md` + `docs/plans/<phase>.md`)? How does the progression plan reference its phases — inline links per phase, naming convention, both? When the planning instance in `scratch/` hands a finalized phase plan to the implementation instance in `assistant/`, what is the literal delivery mechanism? `scratch/CLAUDE.md` says this is settled when phase 1 is planned — phase 1 is where the interface gets answered concretely.
3. **Phase 1 (transcripts) spec via brainstorming.** Durable session-transcript persistence, vault-write primitives, `[cancelled]` marker on disconnect, persistence-round-trip + cancellation evals. Watch for: the progression plan's §13 init checklist (repo scaffold, FastAPI, Logfire, CHANGELOG/NOTES) has not been done either, so phase 1 may presuppose init scaffolding. Flag during brainstorming and decide whether init becomes phase 0 or merges into phase 1.
4. **Deferred follow-ons (still).** `assistant/CLAUDE.md` does not exist. `scratch/CLAUDE.md` now has four stale atelier-plans references (lines 6, 7, 13, 34); line 7 specifically points at `dist/atelier-plans/` which does not exist on disk. Open question I left for Scott at session end and never got an answer to: should `scratch/CLAUDE.md` be updated to reflect the archived skill, or left for later?

## User steering not yet in code

- **NEW — verbatim (sets next session's #1 and #2):** "when we come back, we're going to take one more pass through our new skills and then see how to interface our app progressions with them."
- **NEW — verbatim (definitive intent for the absorption):** "My intent was for you to not reference atleier plans but to incorporate that functionality into writing plans leaving atelier plans untouched and archived." Followed by "A matches what I meant" when given the explicit choice between copy-assets (A) and describe-behavior-only (B).
- **NEW — verbatim (the iteration loop spec, now codified in `writing-plans/SKILL.md` and `html-authoring.md`):** "the user can either give a diff back to the agent and have the agent generate another version of the html or the user can export the markdown from the web planning tool and save it to a plan locally for implementation or for further refinement with the agent - just in markdown form not html."
- **NEW — verbatim (single-artifact rule for this very file):** "i think this might be an addendum, but let's make sure we have a single current tbc-notes to read when we come back."
- **SUPERSEDES:** the prior tbc-notes' Step 1 and Step 2 (revert atelier-plans wiring from brainstorming, then wire it into writing-plans via skill reference). Step 1 is done; Step 2 was overtaken by the absorption — writing-plans no longer references atelier-plans as a skill, it owns the functionality directly.
- **SUPERSEDES:** the plan-mode plan at `~/.claude/plans/actually-would-it-jiggly-phoenix.md`. It was approved on the wire-atelier-plans-by-reference design, then immediately superseded mid-session when Scott directed wholesale absorption.
- **STANDING (carried forward, still binding):** "I'm reading the skills and notice that you've made them specific to this project to the point that they couldn't be used on future projects." Reusable-on-future-projects rule applies to `writing-plans` now that it has the atelier-plans functionality baked in.

## Active state

Nothing mid-flow. No subagents in flight. Last user-facing question I left open and never got an answer to: "Want me to update CLAUDE.md to reflect the new arrangement (drop the `dist/` line, rename 'atelier-plans HTML' to 'writing-plans' HTML form' or similar), or leave it?" That question rolls forward as Step 4.

## Pointers

- `docs/plans/python-llm-app-progression.md` — the 10-phase progression. Source for Step 2 (interface design) and Step 3 (phase 1).
- `.claude/skills/writing-plans/SKILL.md` — has the new `## Interactive HTML Iteration` section. First read on Step 1.
- `.claude/skills/writing-plans/html-authoring.md` — sibling guide written this session. Adapted from `atelier-plans/SKILL.md` with the "this is a skill" framing dropped.
- `.claude/skills/writing-plans/{assets,references}/` — the copied infrastructure. Template + CSS + JS in `assets/`; DESIGN.md + 3 example HTMLs in `references/`.
- `.claude/skills/brainstorming/SKILL.md` — atelier-plans references stripped this session. Read to confirm clean.
- `.claude/archive/atelier-plans/` — moved source skill, untouched. Reference if a question arises about what was absorbed (or what was deliberately not).
- `~/.claude/plans/actually-would-it-jiggly-phoenix.md` — the plan-mode plan from this session. Partially superseded; read for context on the planning conversation only.

## Gotchas not yet in CLAUDE.md or memory

- **The literal string `# Edits to apply (Atelier Plans)` survives in `writing-plans/html-authoring.md` (around line 40).** That is the exact paste-back header the bundled runtime emits when the user clicks Copy diff; the agent parses for it. Don't blanket-rename "atelier-plans" → anything else inside `writing-plans/` without also editing `assets/plan.js` to change the runtime's emitted header AND re-inlining it into `assets/plan-template.html` per the customization steps in `html-authoring.md`. Otherwise the parsing breaks silently.
- **`scratch/CLAUDE.md` has four stale atelier-plans references** (lines 6, 7, 13, 34). Line 7's `dist/atelier-plans/` does not exist on disk. The other three describe a workflow that still happens, just under `writing-plans` now.
- **MIT attribution risk carried forward.** Atelier-plans was separately MIT-licensed (see frontmatter in the archived `SKILL.md`). `writing-plans` now contains its assets without explicit attribution. Scratch is private so this is dormant; if any of those assets ever move into `assistant/` during phase work, the notice-bearing requirement activates. Separately, the four superpowers-derived skills under `assistant/.claude/skills/` also still lack their NOTICES.
- **Background-shell PATH stripping (carried forward from prior session).** Subprocesses spawned from background-Bash see a minimal PATH; pass env directly via `/usr/bin/env PATH=… …`. First place to look if a background command exits 127.
