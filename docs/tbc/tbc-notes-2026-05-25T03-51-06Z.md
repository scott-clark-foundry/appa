---
commit_before_clear: false
---

# To be continued — references-alive reconcile executed; planner caught up to a two-PR drift; one number collision to flag

## Summary (read this first)

This session closed the planner-side drift that built up while the implementer shipped phase 1 (PR #4) plus the unplanned cross-provider tuning DOE (PR #5). The references-alive lifecycle was exercised end-to-end for the first time: implementer-amended references (`jsonl-transcript-format.md`, `vault-write-primitives.md`) cp'd back into scratch as canonical; four newly authored implementer-side references cp'd into scratch (`provider-tuning-doe-2026-05.md` + its `.html` appendix, `pydantic-ai-provider-quirks.md`, `agent-http-patterns.html`). The `02-post-scaffold-iteration.md` retrospective was backported from assistant into scratch, and a parallel `04-tuning-doe-2026-05.md` retrospective was authored fresh in scratch and mirrored to assistant. The progression plan's §Init and §Phase 1 Artifacts lines were rewritten in a new shape (plan link, retrospective link, full reference set, merge markers). All committed on both sides; both pushed. Working hypothesis going forward: planner has zero in-flight work; the next planner cycle starts phase 2.

## User's Mindset

Pure execution mode this session. Five short directives: "Review assistant/ for phase 1 completion. there has been some extra work done you need to get caught up on as well please. take your time. aske youself if the documentation matches the implementation at this time and figure out what we need to do if it does not match." then "reconcile and follow precedent" then "yes" (to commit) then "push" then "are tbc-notes up to date?" Scott trusted the judgment calls on (a) which references to backport, (b) whether to author a 04 retrospective at all, (c) whether to mirror it back to assistant. No second-guessing surfaced. The implicit boundary check ("are tbc-notes up to date?") signals he is about to clear and wants state preserved.

No open user doubts.

## Next steps

The planner has no in-flight work. Next planner session is most likely:

1. **Start phase 2 planning.** Per progression plan, phase 2 is `feat/semantic-recall` (embedding index over completed turns + BM25 alongside, pre-turn retrieval into the system prompt). Likely entry: `journey-planning` skill to define what "semantic recall works" means user-facing, then `writing-plans` for the HTML draft. The phase 2 spec and plan would be `05-semantic-recall.md` (see number-collision gotcha below).
2. **OR pick up an implementer-proposed deviation.** The implementer's most recent tbc proposes `04-developer-ui.md` as the next plan (visualizing the transcripts + DOE data) instead of going straight to semantic recall. Read `assistant/docs/tbc/tbc-notes-2026-05-25T02-57-18Z.md` first; that proposal is worth a real decision rather than a default into semantic recall.
3. **If neither plan is selected,** Settings validator for the `gpt-5.4-mini` + `MODEL_THINKING='minimal'` foot-collision (quirks doc §2) is a one-line trivial fix the implementer flagged as on-deck.

## User steering not yet in code

- **NEW:** "reconcile and follow precedent" — the precedent in question is `02-post-scaffold-iteration.md`: between-phase retrospectives for shipped work live on BOTH sides (scratch + assistant) in `docs/plans/NN-...md`, in retrospective form (no upfront spec, `status: complete`, `spec: n/a`, frontmatter `progression: "progression #lN"` matching the phase the work extended). This now applies to `04-tuning-doe-2026-05.md` as well. Future between-phase spikes should follow the same shape.
- **SUPERSEDES:** Prior tbc's open question about whether `docs/images/` deserves a one-line note in `CLAUDE.md` § Project context — still unaddressed, not raised this session, but the question is unchanged.
- **NEW:** Scott uses `yes` and `push` as one-word confirmations after seeing a proposed plan in the assistant's response. Don't over-interpret terseness — he reads the proposal, agrees, and expects execution. The pattern is: assistant proposes concrete actions with a clear ask, Scott answers in one word, assistant executes without re-confirming.

## Pointers

- `assistant/docs/tbc/tbc-notes-2026-05-25T02-57-18Z.md` — implementer's most recent wrap-up. Its proposed next plan `04-developer-ui.md` collides with my reconcile's use of `04-` (see gotcha §1).
- `docs/plans/python-llm-app-progression.md` §Init and §Phase 1 — Artifacts lines now carry plan, retrospective, references, and merge markers. Use this shape for future phase merges.
- `docs/plans/04-tuning-doe-2026-05.md` — the retrospective I authored; mirrors `02-post-scaffold-iteration.md` shape. Read both side-by-side if writing a future between-phase retrospective.
- `docs/references/provider-tuning-doe-2026-05.md` — load-bearing for phase 2+ if model/cost choices come up. Headline: `gpt-5-nano` + `MODEL_THINKING='minimal'` + `MODEL_EXTRA_SETTINGS='{"openai_text_verbosity":"low"}'` is $0.04/1k chat turns.
- `docs/references/pydantic-ai-provider-quirks.md` — living document; append when new abstraction leaks surface.

## Gotchas not yet in CLAUDE.md or memory

- **Plan number 04 was claimed twice.** I used `04-tuning-doe-2026-05.md` for the tuning retrospective on both sides. The implementer's tbc (`assistant/docs/tbc/tbc-notes-2026-05-25T02-57-18Z.md` Next Steps §2) reserved `04-developer-ui.md` for their next forward plan. The chronological-ordering convention is on my side (the retrospective documents work that shipped before any developer-UI plan exists), but the implementer didn't know that when they wrote their tbc. **Practical consequence:** next forward plan in this project is `05-...`, whether it's developer-UI or semantic-recall. Mention this to the next implementer session if/when picking up either work.

- **Plan numbering is chronological, not phase-aligned.** Numbers `00, 01, 02, 03, 04` map to `init-scaffold (phase 0), ag-ui-migration (phase 0 extension), post-scaffold-iteration (phase 0 extension), transcripts (phase 1), tuning-doe (phase 1 extension)`. The progression plan's "Phase N" numbers do NOT match the file-prefix numbers. If a future planner-side workflow starts auto-numbering, this needs to be a documented convention rather than rediscovered.

- **References-alive lifecycle works as designed but has no automation.** The implementer modifies `assistant/docs/references/X.md`; the planner manually `cp ../assistant/docs/references/X.md docs/references/` at the start of the next planning cycle. Easy to forget; the only signal that anything diverged is reading both sides. A pre-planning checklist or a `bin/refs-pull` shell script would close that gap. Worth a CLAUDE.md addition if it bites once more.

- **The progression plan Artifacts shape is now non-trivial.** It carries plan, retrospective, references, and merge markers. Phase 2 onward should match this shape from the start to avoid the "fix the Artifacts line" reconcile step that this session did for phase 1.
